query EventListWithFollowedQuery($page: Int, $size: Int, $filter: EventFilterInput) {
    events(page: $page, size: $size, filter: $filter) {
        id
        title
        subtitle
        isFollowed
    }
}