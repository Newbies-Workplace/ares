mutation RateLectureMutation($id: String!, $request: LectureRateRequestInput!) {
    rateLecture(id: $id, request: $request) {
        id
        lectureId
        topicRate
        presentationRate
        opinion
        createDate
    }
}