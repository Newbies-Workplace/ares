query UserListQuery($page: Int, $size: Int) {
    users(page: $page, size: $size) {
        id
        nickname
        description
        contact {
            github
            linkedin
            mail
            twitter
        }
        createDate
        updateDate
    }
}