ktor {
  application {
    modules = [ ]
  }
}
jwt {
  realm = "Jeteo Ares"
  issuer = "Jeteo Ares"
  secret = "secret"
}
oauth {
  github {
    userUrl = "https://api.github.com/user"
    clientId = "githubClientId"
    secret = "githubSecret"
  }
}
database {
  driverClassName = "org.mariadb.jdbc.Driver"
  jdbcUrl = "jdbc:mariadb://localhost:3306/ares"
  username = "root"
  password = ""
}